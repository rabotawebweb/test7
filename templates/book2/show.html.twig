{% extends 'base.html.twig' %}

{% block title %}Book2{% endblock %}

{% block body %}
    <h1>Book2</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ book2.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ book2.name }}</td>
            </tr>
            <tr>
                <th>Year</th>
                <td>{{ book2.year }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_book2_index') }}">back to list</a>

    <a href="{{ path('app_book2_edit', {'id': book2.id}) }}">edit</a>

    {{ include('book2/_delete_form.html.twig') }}
{% endblock %}
