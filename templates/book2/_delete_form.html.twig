<form method="post" action="{{ path('app_book2_delete', {'id': book2.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ book2.id) }}">
    <button class="btn">Delete</button>
</form>
