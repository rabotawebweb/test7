{% extends 'base.html.twig' %}

{% block title %}Book2 index{% endblock %}

{% block body %}
    <h1>Book2 index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Year</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for book2 in book2s %}
            <tr>
                <td>{{ book2.id }}</td>
                <td>{{ book2.name }}</td>
                <td>{{ book2.year }}</td>
                <td>
                    <a href="{{ path('app_book2_show', {'id': book2.id}) }}">show</a>
                    <a href="{{ path('app_book2_edit', {'id': book2.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_book2_new') }}">Create new</a>
{% endblock %}
